#include <stdio.h>

int main() {
    // declaração de variáveis
    // Entrada
    int nota1 = 8;
    int nota2 = 7;

    // Processamento
    int media = (nota1 + nota2) / 2;

    // Saída
    printf("A media e %d", media);
    
    return 0;
}